#include <bits/stdc++.h>
using namespace std;
int main()
{
    int ab, c;

    cin >> ab >> c;

    if (c > ab && (c > 0 && c < 90) && (ab > 0 && ab < 90))
    {
        cout << "Olha o Claudio ali em cima!\n";
    }
    else if (c > ab && (c > 90 && c < 10) && (ab > 0 && ab < 90))
    {
        cout << ""
    }
}